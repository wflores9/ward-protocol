SPECIFICATION Spec

\* CONSTANTS are declared in the spec for future extension but are not
\* referenced by any action or invariant in this model. Assign empty sets
\* so TLC accepts the spec without needing to enumerate any extra values.
CONSTANTS
    ClaimId      = {}
    PolicyNFT    = {}
    VaultAddress = {}
    LedgerState  = {}

INVARIANT SafetyInvariant
